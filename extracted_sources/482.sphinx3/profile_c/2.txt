void pctr_print_all (FILE *fp, pctr_t *ctr)
{
    if (! ctr->name)
	return;
    
    fprintf (fp, "CTR:");
    for (; ctr->name; ctr++)
	fprintf (fp, "  %d %s", ctr->count, ctr->name);
    fprintf (fp, "\n");
}
