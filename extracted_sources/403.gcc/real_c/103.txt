static void
eldexp (x, pwr2, y)
     const UEMUSHORT x[];
     int pwr2;
     UEMUSHORT y[];
{
  UEMUSHORT xi[NI];
  EMULONG li;
  int i;

  emovi (x, xi);
  li = xi[1];
  li += pwr2;
  i = 0;
  emdnorm (xi, i, i, li, 64);
  emovo (xi, y);
}
