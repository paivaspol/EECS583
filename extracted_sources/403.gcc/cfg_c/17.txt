void
clear_aux_for_edges ()
{
  int i;

  for (i = -1; i < n_basic_blocks; i++)
    {
      basic_block bb;
      edge e;

      if (i >= 0)
	bb = BASIC_BLOCK (i);
      else
	bb = ENTRY_BLOCK_PTR;

      for (e = bb->succ; e; e = e->succ_next)
	e->aux = NULL;
    }
}
