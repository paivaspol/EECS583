static void
mark_cond_nesting (n)
     struct nesting *n;
{
  while (n)
    {
      ggc_mark_rtx (n->exit_label);
      ggc_mark_rtx (n->data.cond.endif_label);
      ggc_mark_rtx (n->data.cond.next_label);

      n = n->next;
    }
}
