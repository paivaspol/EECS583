void
DNorm(double *vec, int n)
{
  int    x;
  double sum;

  sum = DSum(vec, n);
  if (sum != 0.0) for (x = 0; x < n; x++) vec[x] /= sum;
  else            for (x = 0; x < n; x++) vec[x] = 1. / (double) n;
}
