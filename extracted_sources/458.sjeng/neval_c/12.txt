int  King(int sq,int c) {
  int s = 0;

  if( file(sq) >= 6
   && piecet(sq-1) == rook
   && pieceside(sq-1) == c ) {
    s += 2;
  }

  if( file(sq) >= 6
   && piecet(sq+1) == rook
   && pieceside(sq+1) == c ) {
    s += 2;
  }

  if( c == BLACK )
    s = -s;
  return s;
}
