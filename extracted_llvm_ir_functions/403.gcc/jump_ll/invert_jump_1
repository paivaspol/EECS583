define i32 @invert_jump_1(%struct.rtx_def* %jump, %struct.rtx_def* %nlabel) #0 {
entry:
  %call = tail call i32 @num_validated_changes() #7
  tail call fastcc void @invert_exp_1(%struct.rtx_def* %jump) #8
  %call1 = tail call i32 @num_validated_changes() #7
  %cmp = icmp eq i32 %call1, %call
  br i1 %cmp, label %return, label %if.end

if.end:                                           ; preds = %entry
  %call2 = tail call i32 @redirect_jump_1(%struct.rtx_def* %jump, %struct.rtx_def* %nlabel) #8
  br label %return

return:                                           ; preds = %entry, %if.end
  %retval.0 = phi i32 [ %call2, %if.end ], [ 0, %entry ]
  ret i32 %retval.0
}
