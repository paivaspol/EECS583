define internal fastcc i32 @const_hash_rtx(i32 %mode, %struct.rtx_def* %x) #0 {
entry:
  %value = alloca %struct.rtx_const, align 8
  %0 = getelementptr inbounds %struct.rtx_const* %value, i64 0, i32 0, i64 0
  call void @llvm.lifetime.start(i64 136, i8* %0) #6
  call fastcc void @decode_rtx_const(i32 %mode, %struct.rtx_def* %x, %struct.rtx_const* %value) #10
  %1 = bitcast %struct.rtx_const* %value to i32*
  br label %for.body

for.body:                                         ; preds = %for.body, %entry
  %hi.07 = phi i32 [ 0, %entry ], [ %add, %for.body ]
  %i.06 = phi i64 [ 0, %entry ], [ %inc, %for.body ]
  %arrayidx = getelementptr inbounds i32* %1, i64 %i.06
  %2 = load i32* %arrayidx, align 4, !tbaa !4
  %add = add nsw i32 %2, %hi.07
  %inc = add i64 %i.06, 1
  %exitcond = icmp eq i64 %inc, 34
  br i1 %exitcond, label %for.end, label %for.body

for.end:                                          ; preds = %for.body
  %and = and i32 %add, 1073741823
  %rem = urem i32 %and, 61
  call void @llvm.lifetime.end(i64 136, i8* %0) #6
  ret i32 %rem
}
