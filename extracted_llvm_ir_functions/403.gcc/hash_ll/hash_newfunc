define %struct.hash_entry* @hash_newfunc(%struct.hash_entry* %entry1, %struct.hash_table* %table, i8* nocapture %p) #0 {
entry:
  %cmp = icmp eq %struct.hash_entry* %entry1, null
  br i1 %cmp, label %if.then, label %if.end

if.then:                                          ; preds = %entry
  %call = tail call i8* @hash_allocate(%struct.hash_table* %table, i32 24) #7
  %0 = bitcast i8* %call to %struct.hash_entry*
  br label %if.end

if.end:                                           ; preds = %if.then, %entry
  %entry.addr.0 = phi %struct.hash_entry* [ %0, %if.then ], [ %entry1, %entry ]
  ret %struct.hash_entry* %entry.addr.0
}
