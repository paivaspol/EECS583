define %struct.line_map* @lookup_line(%struct.line_maps* nocapture %set, i32 %line) #0 {
entry:
  tail call void @llvm.dbg.value(metadata !{%struct.line_maps* %set}, i64 0, metadata !67), !dbg !168
  tail call void @llvm.dbg.value(metadata !{i32 %line}, i64 0, metadata !68), !dbg !169
  tail call void @llvm.dbg.value(metadata !9, i64 0, metadata !70), !dbg !170
  %used = getelementptr inbounds %struct.line_maps* %set, i64 0, i32 2, !dbg !170
  %0 = load i32* %used, align 4, !dbg !170, !tbaa !90
  tail call void @llvm.dbg.value(metadata !{i32 %0}, i64 0, metadata !71), !dbg !170
  %cmp = icmp eq i32 %0, 0, !dbg !171
  br i1 %cmp, label %if.then, label %while.cond.preheader, !dbg !171

while.cond.preheader:                             ; preds = %entry
  %cmp114 = icmp ugt i32 %0, 1, !dbg !172
  %maps = getelementptr inbounds %struct.line_maps* %set, i64 0, i32 0, !dbg !173
  %1 = load %struct.line_map** %maps, align 8, !dbg !173, !tbaa !99
  br i1 %cmp114, label %while.body, label %while.end, !dbg !172

if.then:                                          ; preds = %entry
  tail call void @abort() #8, !dbg !175
  unreachable, !dbg !175

while.body:                                       ; preds = %while.cond.preheader, %while.body
  %mx.016 = phi i32 [ %div.mx.0, %while.body ], [ %0, %while.cond.preheader ]
  %mn.015 = phi i32 [ %mn.0.div, %while.body ], [ 0, %while.cond.preheader ]
  %add = add i32 %mx.016, %mn.015, !dbg !176
  %div = lshr i32 %add, 1, !dbg !176
  tail call void @llvm.dbg.value(metadata !{i32 %div}, i64 0, metadata !69), !dbg !176
  %idxprom = zext i32 %div to i64, !dbg !173
  %from_line = getelementptr inbounds %struct.line_map* %1, i64 %idxprom, i32 2, !dbg !173
  %2 = load i32* %from_line, align 4, !dbg !173, !tbaa !90
  %cmp2 = icmp ugt i32 %2, %line, !dbg !173
  %mn.0.div = select i1 %cmp2, i32 %mn.015, i32 %div, !dbg !177
  %div.mx.0 = select i1 %cmp2, i32 %div, i32 %mx.016, !dbg !177
  %sub = sub i32 %div.mx.0, %mn.0.div, !dbg !172
  %cmp1 = icmp ugt i32 %sub, 1, !dbg !172
  br i1 %cmp1, label %while.body, label %while.cond.while.end_crit_edge, !dbg !172

while.cond.while.end_crit_edge:                   ; preds = %while.body
  %phitmp = zext i32 %mn.0.div to i64, !dbg !172
  br label %while.end, !dbg !172

while.end:                                        ; preds = %while.cond.preheader, %while.cond.while.end_crit_edge
  %mn.0.lcssa = phi i64 [ %phitmp, %while.cond.while.end_crit_edge ], [ 0, %while.cond.preheader ]
  %arrayidx7 = getelementptr inbounds %struct.line_map* %1, i64 %mn.0.lcssa, !dbg !178
  ret %struct.line_map* %arrayidx7, !dbg !178
}
