define internal fastcc i32 @target_char_cast(%union.tree_node* %cst, i8* nocapture %p) #0 {
entry:
  %call = tail call i32 @host_integerp(%union.tree_node* %cst, i32 1) #7
  %tobool = icmp eq i32 %call, 0
  br i1 %tobool, label %return, label %if.end

if.end:                                           ; preds = %entry
  %call1 = tail call i64 @tree_low_cst(%union.tree_node* %cst, i32 1) #7
  %conv = trunc i64 %call1 to i8
  store i8 %conv, i8* %p, align 1, !tbaa !1
  br label %return

return:                                           ; preds = %entry, %if.end
  %retval.0 = phi i32 [ 0, %if.end ], [ 1, %entry ]
  ret i32 %retval.0
}
