define void @free_after_parsing(%struct.function* %f) #0 {
entry:
  %0 = load void (%struct.function*)** @free_lang_status, align 8, !tbaa !0
  %tobool = icmp eq void (%struct.function*)* %0, null
  br i1 %tobool, label %if.end, label %if.then

if.then:                                          ; preds = %entry
  tail call void %0(%struct.function* %f) #10
  br label %if.end

if.end:                                           ; preds = %entry, %if.then
  tail call void @free_stmt_status(%struct.function* %f) #10
  ret void
}
