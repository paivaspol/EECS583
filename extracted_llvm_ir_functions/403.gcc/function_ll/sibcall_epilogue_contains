define i32 @sibcall_epilogue_contains(%struct.rtx_def* nocapture %insn) #4 {
entry:
  %0 = load %struct.varray_head_tag** @sibcall_epilogue, align 8, !tbaa !0
  %tobool = icmp eq %struct.varray_head_tag* %0, null
  br i1 %tobool, label %return, label %if.then

if.then:                                          ; preds = %entry
  %call = tail call fastcc i32 @contains(%struct.rtx_def* %insn, %struct.varray_head_tag* %0) #9
  br label %return

return:                                           ; preds = %entry, %if.then
  %retval.0 = phi i32 [ %call, %if.then ], [ 0, %entry ]
  ret i32 %retval.0
}
