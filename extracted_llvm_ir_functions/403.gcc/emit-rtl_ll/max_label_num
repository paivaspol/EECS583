define i32 @max_label_num() #5 {
entry:
  %0 = load i32* @last_label_num, align 4, !tbaa !0
  %tobool = icmp ne i32 %0, 0
  %.pre = load i32* @label_num, align 4, !tbaa !0
  %1 = load i32* @base_label_num, align 4, !tbaa !0
  %cmp = icmp eq i32 %.pre, %1
  %or.cond = and i1 %tobool, %cmp
  %retval.0 = select i1 %or.cond, i32 %0, i32 %.pre
  ret i32 %retval.0
}
