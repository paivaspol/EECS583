define internal void @df_lr_transfer_function(i32 %bb, i32* nocapture %changed, %struct.bitmap_head_def* %in, %struct.bitmap_head_def* %out, %struct.bitmap_head_def* %use, %struct.bitmap_head_def* %def, i8* nocapture %data) #0 {
entry:
  %call = tail call i32 @bitmap_union_of_diff(%struct.bitmap_head_def* %in, %struct.bitmap_head_def* %use, %struct.bitmap_head_def* %out, %struct.bitmap_head_def* %def) #8
  store i32 %call, i32* %changed, align 4, !tbaa !4
  ret void
}
